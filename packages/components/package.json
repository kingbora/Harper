{
  "name": "@cus/components",
  "version": "0.0.0",
  "scripts": {
    "test": "echo \"Error: run tests from root\" && exit 1",
    "build": "ts-node ./build.ts",
    "watch": "ts-node ./build.ts -watch",
    "lint": "tsc --noEmit"
  }
}
